import ReactDOM from 'react-dom';
import './styles.scss';
import App from './App';
import React from 'react';

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);
